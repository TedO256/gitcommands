git init  - This command is used to turn a folders into a git repo
git status - Used to check the status of the work we've done inside of the folder
git add <filename> - Used to stage work from the work are to the staging area
git commit -m "message" - Used to create history of our work
git log - Used to check the list of all of our commit
